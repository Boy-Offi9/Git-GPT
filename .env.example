GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
NEXT_PUBLIC_APP_URL=http://localhost:3000
SESSION_SECRET=
DATABASE_URL=
# Optional: set to 0 to disable the in-process crawler worker (use `npm run crawler:worker` instead)
# CRAWLER_WORKER_IN_PROCESS=0
